import React from "react";

// Country selector is currently fixed to Hong Kong only.
// This component is kept as a placeholder in case more jurisdictions are added in the future.
const ChooseCountry: React.FC = () => {
  return null;
};

export default ChooseCountry;
